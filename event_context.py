"""
事件背景信息 — 韩宇飞 整理

每个 event 对应一个真实新闻事件，用于 LLM 生成解释时提供上下文。
背景信息应包含：事件主题、关键事实、争议焦点。

⚠️ 当前为基于训练数据推文内容的初步判断，可能需要根据实际新闻进一步确认。
"""

EVENT_CONTEXT = {
    0: (
        "该事件涉及德国收藏家 Cornelius Gurlitt 的艺术藏品归属争议。"
        "这批藏品中发现有二战时期纳粹掠夺的艺术品，引发了关于归还问题的广泛讨论。"
        "瑞士伯尔尼美术馆（Kunstmuseum Bern）宣布接受这批赠予，"
        "同时承诺调查藏品的来源，疑似纳粹掠夺品将留在德国处理。"
    ),

    1: (
        "该事件涉及美国密苏里州 Ferguson 市警察执法争议。"
        "2014年8月，一名未携带武器的黑人少年 Michael Brown 被白人警察 Darren Wilson 枪击身亡，"
        "引发大规模抗议和社会动荡。事件中流传大量未经证实的目击者说法，"
        "关于警察是否有正当理由、是否有过度执法、是否存在种族歧视等问题争议激烈。"
    ),

    2: (
        "该事件样本极少（训练集仅9条），且全部标注为谣言。"
        "从推文内容推测，涉及一起引起广泛关注的公共事件，"
        "但具体背景信息有限，建议结合更多外部资料确认。"
    ),

    3: (
        "该事件训练集中几乎全部推文被标注为谣言（99%），"
        "表明该事件中存在大量未经证实的信息传播。"
        "具体事件主题需结合推文内容进一步确认，"
        "但从标注分布可以推断这是一个容易滋生谣言的争议性事件。"
    ),

    4: (
        "该事件谣言与非谣言分布较为均匀（约各50%），"
        "表明该事件的推文信息可信度参差不齐。"
        "具体事件主题需结合推文内容进一步确认。"
    ),

    5: (
        "该事件是数据集中样本量最大的事件（训练集854条），"
        "谣言占比约43%。具体事件主题需结合推文内容进一步确认，"
        "从规模来看这可能是一个引起广泛社会讨论的新闻事件。"
    ),

    6: (
        "该事件样本量较大（训练集623条），谣言占比约53%。"
        "推文中包含关于 Ottawa 的提及，可能与加拿大首都发生的某起事件相关。"
        "具体事件主题需结合推文内容进一步确认。"
    ),
}


def get_event_context(event_id: int) -> str:
    """获取事件背景文本"""
    return EVENT_CONTEXT.get(event_id, "暂无该事件的背景信息。")


def get_context_summary(event_id: int) -> str:
    """获取事件背景摘要（短版，用于 prompt 节省 token）"""
    ctx = EVENT_CONTEXT.get(event_id, "")
    if len(ctx) > 100:
        return ctx[:100] + "..."
    return ctx


if __name__ == '__main__':
    for eid in range(7):
        print(f"Event {eid}:")
        print(f"  {get_event_context(eid)}")
        print()
